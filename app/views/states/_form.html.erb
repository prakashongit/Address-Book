<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <p>Country     <select name="country_id">
      <%= @country.each do |country| %>
          <option value="<%= country.id %>"><%= country.name %></option>
      <% end %>
    </select></p>
    <p>State Name <%= f.text_field :name %></p>
    <p> <%= f.check_box :is_Active %>Active</p>


    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
